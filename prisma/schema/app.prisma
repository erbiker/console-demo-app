model App {
    id   String @id @default(cuid())
    name String

    url  String?
    logo String?

    createdAt DateTime  @default(now())
    updatedAt DateTime  @updatedAt
    deletedAt DateTime?
}
